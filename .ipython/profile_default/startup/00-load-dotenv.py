# Auto-load the project's .env into this kernel on startup.
# Set up by the workshop so API keys are available without running load_dotenv() manually.
from dotenv import load_dotenv
load_dotenv("/Users/datasense/Desktop/langgrapgh-agent/.env")
